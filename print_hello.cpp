#include <bits/stdc++.h>
using namespace std;

int main(){
    int n = 7;
    vector<int> a(n, -1), b(n, n);
    for (int i=0;i<n;i++) {
        cout << "A: " << a[i] << endl;
        cout << "B: " << b[i] << endl;
    }

    return 0;
}